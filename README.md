# Сервис сбора баллов ЕГЭ (FastAPI + Telegram Bot)

Прототип сервиса, который позволяет ученикам регистрироваться и вводить баллы ЕГЭ через Telegram-бота.  
Бизнес-логика реализована через FastAPI, база данных — PostgreSQL.  

---

## 📦 Структура проекта

project_root/
│── app/ # FastAPI код
│── migrations/ # Alembic миграции
│── bot/ # Telegram-бот (aiogram v3)
│── Dockerfile.api
│── Dockerfile.bot
│── docker-compose.yml
│── .env.example
│── requirements.txt


## ⚡ Требования

- Docker
- Docker Compose
- Python 3.11+ (для локальной разработки)

## 🛠 Настройка

1. Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
Укажите свои данные в .env:

env
Копировать код
# .env
BOT_TOKEN=ваш_токен_бота
DATABASE_URL=postgresql+psycopg2://postgres:12345@db:5432/umschoolkz
API_URL=http://api:8000
Не коммитьте .env в GitHub!

🐳 Запуск через Docker
Поднять все сервисы:

bash
Копировать код
docker-compose up --build
Сервисы:

db — PostgreSQL

api — FastAPI

bot — Telegram Bot

✅ Проверка работы
FastAPI: http://localhost:8000/docs — Swagger UI для проверки эндпоинтов.

Telegram Bot: использовать команды:

/start — приветствие

/register — регистрация

/enter_scores — ввод баллов

/view_scores — просмотр баллов

🗂 Миграции базы данных
Если добавлены новые модели или поля:

docker-compose run api alembic revision --autogenerate -m "описание"
docker-compose run api alembic upgrade head
📝 Примечания
Токены и пароли хранятся только в .env

Все зависимости для FastAPI и бота описаны в requirements.txt

При работе через Docker localhost внутри контейнера не тот же, что на хосте, для связи контейнеров использовать имена сервисов (db, api)

